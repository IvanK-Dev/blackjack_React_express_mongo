const { checkExistingToken } = require('./checkExistingToken');
const { checkPlayersCuontity } = require('./checkPlayersCuontity');
const { checkToken } = require('./checkToken');

module.exports = { checkExistingToken, checkPlayersCuontity, checkToken };
